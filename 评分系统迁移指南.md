# RAG 推荐评分系统 · 迁移集成指南

> **用途**：把本项目的「RAG 证据驱动 7 维可解释评分系统」迁移到**任何推荐型 RAG 项目**（电商/内容/招聘/课程…）。
> **读完能动手**：明确前提条件、输入输出契约、核心算法、适配清单、代码骨架、调参方法。
> **配套源码**：[scoring.py](backend/app/decision/scoring.py)、[evidence_metrics.py](backend/app/decision/evidence_metrics.py)

---

## 0. 这套评分是什么（30 秒）

一套**确定性的、证据驱动的推荐评分引擎**，插在「检索/重排」之后、「LLM 生成回复」之前：

```
检索 → 重排(Reranker) → 【⭐ 本评分系统 】→ LLM 生成推荐语 → 返回用户
```

**核心思想**：把「数值打分」这件 LLM 不擅长的事（不稳定、会幻觉、贵、黑盒），交给确定性规则；LLM 只负责生成自然语言。评分输出**可解释、可溯源、可复现、无幻觉**。

**评分三层结构**：
1. **6 维加权主公式** → 基础分
2. **证据置信度压顶** → 防止"分高但无依据"
3. **硬约束一票否决** → 不合格直接出局

---

## 1. 前提条件（你的项目要有啥）⭐

> 迁移前先对照检查，决定哪些维度能用、哪些要降级。

### 🟢 硬性前提（没有就跑不起来）

| # | 前提 | 说明 | 没有的后果 |
|---|---|---|---|
| 1 | **RAG 检索链路** | 评分是"检索后打分"，必须有候选物品流入 | 无法使用，这套不是独立推荐器 |
| 2 | **候选物品有 `price` 字段** | budget_fit / value_score 依赖 | 价格相关维度失效 |
| 3 | **候选物品有文本信息**（title/description） | spec_quality / scenario_fit 依赖 | 这两维退化 |
| 4 | **Python 3.10+** | 用了 `float \| None` 等新语法 | 语法错误 |

### 🟡 强烈建议（没有能跑，但效果打折）

| # | 前提 | 用于 | 没有的降级方案 |
|---|---|---|---|
| 5 | **Reranker（cross-encoder）** | relevance 维度（权重最大 0.45） | 降级为向量余弦相似度，精度下降 |
| 6 | **物品评论数据（rating）** | user_sat / risk_penalty / value 的品质分 | 走默认值（如无评论=0.80），口碑/风险维度变平 |
| 7 | **子品类字段（sub_category）** | value_score 的品类基准 | 性价比维度退化（无基准可比） |
| 8 | **chunk 级证据** | evidence_confidence | 证据置信度恒定，失去"压顶"保护 |
| 9 | **意图拆解（预算/场景）** | budget_fit / scenario_fit 的用户侧输入 | 这两维用默认值（无约束=高分） |

### 🔴 建议先评估的

- **你的领域是否"决策型"**：多约束、需可解释、风险敏感 → 适合；纯知识问答 → 用不上，别硬搬。
- **物品规模**：< 1000 件可直接用；百万级需把"检索截断 top_k"调大，否则打分无筛选空间（见第 8 节坑）。

---

## 2. 输入 / 输出契约

### 输入（评分函数需要什么）

```python
score(
    product = {
        "product_id": "P001",
        "title": "...",
        "brand": "...",
        "category": "数码",          # 大类
        "sub_category": "真无线耳机", # 子类(性价比基准用)
        "price": 1299,
        "description": "...",         # 规格品质用
        "reviews": [                  # 口碑/风险用(可选)
            {"rating": 5, "content": "..."},
            {"rating": 2, "content": "..."},
        ],
    },
    query = "2000以内降噪耳机",
    budget_max = 2000,        # 用户预算(可选,来自意图拆解)
    scenario = "business_trip", # 用户场景(可选)
    reranker_score = 0.85,    # ⭐ Reranker 分(核心,复用检索阶段结果)
    evidence_metrics = {...}, # 证据指标(见 5.3)
    spec_keywords = ["降噪","续航"],  # LLM提取的规格词(可选)
)
```

### 输出（评分函数给什么）

```python
DecisionResult = {
    "final_score": 0.88,            # 0~1 最终分
    "display_score": 8.8,           # 0~10 展示分
    "recommendation_level": "strong_recommend",
        # strong_recommend / recommended / cautious
        # / insufficient_evidence / not_recommended
    "evidence_confidence": 0.72,    # 证据置信度
    "component_scores": {           # ⭐ 每维拆解(可解释核心)
        "relevance":   {"score":0.88, "weight":0.45, "method":"reranker", "evidence_ids":["E-1"]},
        "budget_fit":  {"score":0.93, "weight":0.20, "method":"price_rule"},
        "user_sat":    {"score":0.86, "weight":0.12, "method":"bayesian_smooth"},
        "value_score": {"score":0.85, "weight":0.10, "method":"category_benchmark"},
        "spec_quality":{"score":0.90, "weight":0.08, "method":"spec_richness"},
        "scenario_fit":{"score":0.78, "weight":0.05, "method":"keyword_match"},
        "risk_penalty":{"score":0.02, "weight":null,  "evidence_ids":["R-2"]},
    },
    "evidence_ids": ["E-1","R-1","POL-1"],  # ⭐ 支撑证据ID(可溯源)
    "risk_factors": ["有1条差评"],
    "recommendation_reason": "强烈推荐 | XX品牌...",
}
```

---

## 3. 核心算法（要搬的内核）

### 3.1 第一层：6 维加权主公式

```
raw = 0.45×relevance      # 语义相关度(复用Reranker分)
    + 0.20×budget_fit     # 价格合适度
    + 0.12×user_sat       # 用户口碑(贝叶斯平滑)
    + 0.10×value_score    # 性价比(子品类基准)
    + 0.08×spec_quality   # 规格品质
    + 0.05×scenario_fit   # 场景适配
    + preference_bonus    # 偏好加成(≤0.10)
    − risk_penalty        # 风险扣分(≤0.20)
    − avoid_penalty       # 避雷惩罚(≤0.10)

final = clamp(raw, 0, 1)
display = round(final × 10, 1)
```

### 3.2 各维算法速查

| 维度 | 公式要点 |
|---|---|
| **relevance** | 取 Reranker 分；校准曲线 `0.68 + 0.38×score` |
| **budget_fit** | 无约束→0.98；≤预算按 `price/budget` 比给 0.92~0.98；>预算按超支扣 |
| **user_sat** | `avg(rating)/5` → **贝叶斯平滑** `(s×n + 0.80×3)/(n+3)` → 评论数/好评词奖励 |
| **value_score** | `品质系数×(0.5×品质分 + 0.5×价格分)`，品质分来自评论，价格分对比子品类中位价 |
| **spec_quality** | LLM spec_keywords 优先；兜底数描述里规格信号密度 |
| **scenario_fit** | 基线 0.70 + query/场景关键词在文本命中加分 |
| **risk_penalty** | 差评(≤2星)数 + 均分低 → 扣分，**绑定差评 evidence_id** |

### 3.3 第二层：证据置信度压顶

```
evidence_confidence = 0.25×相关度 + 0.20×来源覆盖 + 0.20×维度覆盖
                    + 0.15×来源质量 + 0.10×可靠度 + 0.10×一致性
```

按 final_score + evidence_confidence 定 5 档，再用 cap 压顶：

| 档位 | 条件 | 分数上限 |
|---|---|---|
| strong_recommend | 分≥0.80 且 证据≥0.50 且 风险<0.10 | 不压 |
| recommended | 分≥0.65 | 不压 |
| cautious | 分≥0.55 或 风险≥0.20 | cap 0.80 |
| insufficient_evidence | 证据置信度<0.25 | **cap 0.50** |
| not_recommended | 分<0.55 | **cap 0.45** |

### 3.4 第三层：硬约束一票否决

品类不符 / 价格超预算 2 倍 → 强制 `final≤0.45`、`level=not_recommended`。

---

## 4. 适配清单（哪些直接搬 / 哪些必须改）⭐

> 迁移的核心工作量在这张表。

### ✅ 可直接搬（领域无关，照抄）

- 6 维加权公式**结构**与权重（除非你业务优先级不同）
- **贝叶斯平滑**公式 `(s×n + 0.80×3)/(n+3)`
- Reranker 分**校准曲线** `0.68 + 0.38×score`
- recommendation_level **5 档分档逻辑** + confidence_cap
- evidence_confidence **计算框架**（7 个指标的加权）
- 硬约束一票否决机制

### 🔧 必须改（领域相关，最大工作量）

| 要改的 | 原值（电商） | 你要做什么 |
|---|---|---|
| **`CATEGORY_BENCHMARKS` 子品类基准价** | 28 个电商子品类（耳机800/手机4000…） | ⭐**最重**：为你的每个子品类标定 `(中位价, 品质系数)` |
| **场景关键词字典** `scenario_kw_map` | flight/commute/sport… | 换成你的领域场景（如招聘：远程/出差/管理） |
| **好评关键词** | 推荐/好用/满意/回购… | 换成你的领域好评词 |
| **spec_richness 正则** | ml/g/W/mAh/IP68… | 换成你的规格信号（服装：尺码/面料；课程：课时/level） |
| **证据类型映射** `_SOURCE_TYPE_MAP` | product_profile/faq/review… | 换成你的 chunk 类型 |
| **权重 0.45/0.20/...** | 电商：相关度最重要 | 按你业务调（如招聘可能"匹配度"权重更高） |

### ⚙️ 建议改（调优）

- 贝叶斯先验 `prior=0.80`、`C=3`：评论普遍少的领域调低 prior
- 分档分数线 `0.80/0.65/0.55`：按你分数分布调
- 置信度阈值 `0.25/0.50`：按你证据丰富度调

---

## 5. 代码骨架（怎么写）

> 精简版可直接用，照着填你的领域逻辑。

### 5.1 数据结构（最小字段集）

```python
from dataclasses import dataclass, field

@dataclass
class Product:
    product_id: str
    title: str
    brand: str = ""
    category: str = ""           # 大类
    sub_category: str = ""       # 子类(性价比基准)
    price: float = 0.0
    description: str = ""        # 规格品质/场景匹配
    reviews: list[dict] = field(default_factory=list)
        # [{"rating": 1-5, "content": "..."}]
```

### 5.2 核心评分类（直接抄 + 改基准表）

```python
import math

# ⭐ 必须改成你的子品类基准 (中位价, 品质系数)
CATEGORY_BENCHMARKS = {
    # 电商示例:
    "真无线耳机": (800, 1.1),
    "智能手机": (4000, 0.9),
    # ↓ 换成你的领域, 例如招聘:
    # "后端开发": (30000, 1.0),   # 月薪中位, 品质系数
}

class RecommendScoring:

    def score(self, product, query, budget_max=None,
              reranker_score=0.0, evidence_confidence=0.5):
        # ① 6 维
        relevance   = self._relevance(reranker_score, product, query)
        budget_fit  = self._budget_fit(product.price, budget_max)
        user_sat    = self._user_sat(product.reviews)
        value       = self._value_score(product)
        spec        = self._spec_quality(product.description)
        scenario    = self._scenario_fit(product, query)
        risk, risk_ids = self._risk_penalty(product.reviews, product.product_id)

        raw = (0.45*relevance + 0.20*budget_fit + 0.12*user_sat
               + 0.10*value + 0.08*spec + 0.05*scenario - risk)
        final = max(0.0, min(1.0, raw))

        # ② 证据置信度压顶
        level = self._level(final, evidence_confidence, risk)
        final = self._cap(final, level)

        return {
            "final_score": round(final, 4),
            "display_score": round(final * 10, 1),
            "recommendation_level": level,
            "component_scores": {  # ⭐ 可解释核心
                "relevance": relevance, "budget_fit": budget_fit,
                "user_sat": user_sat, "value": value,
                "spec": spec, "scenario": scenario, "risk": risk,
            },
            "risk_evidence_ids": risk_ids,
        }

    # ---- 各维实现 ----
    def _relevance(self, reranker_score, product, query):
        if reranker_score > 0:
            return min(1.0, 0.68 + 0.38 * reranker_score)  # 校准曲线
        return 0.62  # 无 Reranker 降级

    def _budget_fit(self, price, budget_max):
        if not budget_max: return 0.98
        if price <= budget_max: return 0.92 + 0.06 * (1 - price/budget_max)
        over = (price - budget_max) / budget_max
        return max(0.0, 0.80 - over) if over < 0.5 else max(0.0, 0.45 - over)

    def _user_sat(self, reviews):
        if not reviews: return 0.80
        s = sum(r["rating"] for r in reviews) / len(reviews) / 5.0
        # ⭐ 贝叶斯平滑
        return (s * len(reviews) + 0.80 * 3) / (len(reviews) + 3)

    def _value_score(self, product):
        median, q_mult = CATEGORY_BENCHMARKS.get(
            product.sub_category, (product.price, 1.0))
        quality = 0.55 + 0.45 * (self._avg_rating(product.reviews) / 5.0)
        price_score = 0.95 if product.price <= median*0.5 else \
                      0.82 if product.price <= median else 0.58
        return min(1.0, max(0.3, q_mult * (0.5*quality + 0.5*price_score)))

    def _spec_quality(self, description):
        # ⭐ 改成你的规格信号正则
        import re
        signals = len(re.findall(r'\d+\.?\d*\s*(ml|g|kg|w|hz|mah|寸|码|课时)', description))
        return min(0.96, 0.74 + signals * 0.04)

    def _scenario_fit(self, product, query):
        # ⭐ 改成你的场景关键词字典
        SCENE_KW = {"出差": ["便携","航空"], "运动": ["防水","轻量"]}
        text = (product.title + product.description).lower()
        hits = sum(1 for kw in [w for v in SCENE_KW.values() for w in v] if kw in text)
        return min(1.0, 0.70 + hits * 0.08)

    def _risk_penalty(self, reviews, pid):
        bad = [r for r in reviews if r["rating"] <= 2]
        penalty = 0.08 if len(bad) >= 3 else (0.02 if bad else 0.0)
        ids = [f"R-{pid}-{i}" for i, r in enumerate(reviews) if r["rating"] <= 2]
        return min(0.20, penalty), ids

    def _level(self, final, ev_conf, risk):
        if ev_conf < 0.25: return "insufficient_evidence"
        if risk >= 0.20:   return "cautious"
        if final >= 0.80 and ev_conf >= 0.50 and risk < 0.10: return "strong_recommend"
        if final >= 0.65:  return "recommended"
        if final >= 0.55:  return "cautious"
        return "not_recommended"

    def _cap(self, final, level):
        return {"not_recommended": 0.45, "insufficient_evidence": 0.50,
                "cautious": 0.80}.get(level, final) and min(
                final, {"not_recommended":0.45,"insufficient_evidence":0.50,"cautious":0.80}.get(level, 1.0))

    def _avg_rating(self, reviews):
        return sum(r["rating"] for r in reviews) / len(reviews) if reviews else 3.0
```

### 5.3 证据置信度（可选，但有它才完整）

```python
def evidence_confidence(evidence_list):
    """evidence_list: [{"source_type":"faq","score":0.9}, ...]"""
    types = {e["source_type"] for e in evidence_list}
    # 来源覆盖: 有几种不同类型证据
    coverage = len(types & {"profile","faq","review"}) / 3
    # 来源质量: 不同类型可信度
    weights = {"sku":0.95,"faq":0.90,"profile":0.85,"review":0.75,"marketing":0.60}
    quality = sum(weights.get(t,0.5) for t in types) / max(len(types),1)
    # 相关度: 最高证据分
    relevance = max((e.get("score",0) for e in evidence_list), default=0)
    return 0.30*relevance + 0.40*coverage + 0.30*quality
```

### 5.4 调用示例

```python
scorer = RecommendScoring()

# 检索 + 重排后, 对每个候选打分
for product in reranked_candidates:
    result = scorer.score(
        product=product,
        query=user_query,
        budget_max=user_budget,        # 来自意图拆解
        reranker_score=product.reranker_score,  # 来自 Reranker
        evidence_confidence=evidence_confidence(product.evidence),
    )
    product.final_score = result["final_score"]
    product.level = result["recommendation_level"]

# 按分排序展示
ranked = sorted(reranked_candidates, key=lambda p: p.final_score, reverse=True)
```

---

## 6. 调参指南

| 参数 | 默认 | 调参建议 |
|---|---|---|
| **6 维权重** | .45/.20/.12/.10/.08/.05 | 按业务优先级：什么最重要就给最高权重。A/B 测试调 |
| **贝叶斯 prior** | 0.80 | 评论普遍少 → 降到 0.70；评论多 → 升到 0.85 |
| **贝叶斯 C** | 3 | 样本量普遍小 → 增大到 5；样本量大 → 降到 2 |
| **分档线** | .80/.65/.55 | 跑一遍看分数分布，按分位数设定（如 top20%=strong） |
| **品质系数** | 0.9~1.5 | 品类越"便宜就能买到好货"系数越高（如饮料1.5） |
| **Reranker 校准** | 0.68+0.38x | 若你的 Reranker 分分布不同，重新拟合截距斜率 |

---

## 7. 迁移步骤 Checklist

```
□ 1. 对照【前提条件】评估你的项目, 决定哪些维度可用/降级
□ 2. 定义 Product 数据结构(至少 product_id/title/price/description/reviews)
□ 3. ⭐ 标定 CATEGORY_BENCHMARKS(为每个子品类定中位价+品质系数)
□ 4. 替换领域关键词(场景字典/好评词/spec正则/证据类型映射)
□ 5. 接入 Reranker 分(或降级为余弦相似度)
□ 6. 实现证据置信度(若有 chunk 级证据)
□ 7. 跑一批真实 query, 看分数分布, 调权重/分档线
□ 8. 接入 LLM 生成(把 component_scores 喂给 LLM 写推荐语)
□ 9. (可选)建评测集, 对比"LLM直接打分" vs "规则评分"的稳定性
```

---

## 8. 常见坑

1. **子品类基准不标定就上线** → 性价比维度全是默认值，等于失效。**这是最大的迁移工作量，别偷懒**。
2. **检索截断太早**（如 top_k=5）→ 打分失去"从多挑少"的筛选空间，退化成"几个里排序"。大规模数据建议检索召回 20-30，让评分有筛选余地。
3. **chunk 级无相关度阈值** → 低分证据混入噪音。建议召回后加一道余弦 ≥0.3 过滤。
4. **把 Reranker 分当 LLM 分讲** → Reranker 是 cross-encoder 判别模型，不是生成式 LLM，别混。
5. **权重照搬不调** → 电商权重未必适合你的领域，必须按业务重定。
6. **没有评测就上线** → 评分是规则系统，必须有 golden set 量化（Recall@K/MRR/NDCG），否则不知道调参对不对。

---

## 9. 一句话总结

> **这套评分 = 「6 维加权 + 证据置信度压顶 + 硬约束否决」三层确定性规则。迁移时：公式结构/贝叶斯/校准曲线/分档逻辑直接搬；子品类基准价、领域关键词、权重必须按你的项目重定。最大工作量是标定 `CATEGORY_BENCHMARKS`。**

---

**源码参考**：
- 主公式与各维算法：[scoring.py](backend/app/decision/scoring.py)
- 证据指标与置信度：[evidence_metrics.py](backend/app/decision/evidence_metrics.py)
- 编排接入示例：[decision_agent.py](backend/app/agents/decision_agent.py)
