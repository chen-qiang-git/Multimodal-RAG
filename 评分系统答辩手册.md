# OmniCart 评分系统 · 答辩深度问答手册

> 用途：面试/答辩现场速查。每个问题按"面试官真实会问"设计，答案精炼 + 带代码证据。
> 原则：**先讲结论（金句），再讲怎么做的，最后给代码位置。** 每个答案控制在 1 分钟内讲完。
>
> ⚠️ 开讲前必看 [防翻车红线](#-防翻车红线必读)，里面有 4 个数字/口径容易被现场抓包。

---

## 🎯 0. 30 秒电梯陈述（开场必背）

> "OmniCart 的推荐评分是一套 **RAG 证据驱动的可解释决策系统**，经过三代演进——从 jieba 关键词，到让 LLM 直接打分，再到现在的「规则 7 维 + Reranker 分」。核心思路是：**把『数值打分』这件 LLM 不擅长的事（不稳定、会幻觉、贵、黑盒）交给确定性规则，LLM 只负责它擅长的理解（意图/图像）和生成（推荐文案）**。最终评分 = 6 维加权 + 证据置信度压顶 + 硬约束一票否决，三层兜底，每个分数都绑定 evidence_id 可溯源、无幻觉。"

**一句话锚点**：传统 RAG 是「检索答案」，我们是「检索 + 决策」；多一个打分环节 = 给决策型问答装了 RAG 缺失的"裁判大脑"。

---

## 📦 模块一：架构与分工

### Q1. 这个评分系统在整个架构里处于什么位置？

全链路是 LangGraph StateGraph 编排的 5 个 Agent + 2 个节点：

```
用户输入 → Router → [Visual?] → Retrieval → Reranker → EvidenceCheck → Decision → Response → SSE返回
         (意图)    (识图)      (召回)       (重排)     (证据检查)      (⭐打分)    (生成文案)
```

- **打分发生在 Decision 环节**，输入是 Reranker 重排后的候选商品，输出是 `final_score(0~1)` / `display_score(0~10)`。
- 代码：编排 [graph.py](backend/app/workflow/graph.py)，评分 [scoring.py](backend/app/decision/scoring.py)。

### Q2. 五个 Agent 分别干什么？LLM 在哪用、哪不用？

| Agent | 用 LLM 吗 | 职责 |
|---|---|---|
| **Router** | ✅ 用 | 意图识别 + 约束提取（品类/预算/场景） |
| **Visual** | ✅ 用（Qwen-VL） | 拍照识图 + 品类映射 + DB 精确匹配 |
| **Retrieval** | ❌ 不用（Embedding 是判别模型） | 多路证据检索（文本+评论+FAQ） |
| **Reranker** | ⚠️ 非生成式 / FAST 模式可跳过 | 语义精排，产出 `reranker_score` |
| **Decision** | ❌ **算分不用** / LLM 评估默认关 | 6 维加权规则评分 |
| **Response** | ✅ 用 | 把推荐结论写成自然语言文案 |

**金句**：LLM 干它擅长的（理解、生成），规则干 LLM 不擅长的（确定性算分）。这叫 **neuro-symbolic 混合架构（神经+符号分工）**。

### Q3. 前后端技术栈？

- **后端**：Python + FastAPI（异步）+ LangGraph 编排 + Qwen 全栈模型（VL/Chat/Embedding/Reranker）+ Qdrant 向量库 + PostgreSQL + Redis。
- **客户端**：Android 原生（Kotlin + Jetpack Compose + Material 3 + MVVM），SSE 流式。**注意叫"Android 客户端"不叫"前端/移动端"**（只有 Android，无 iOS、无 Web）。

---

## 🔄 模块二：三代演进（最能体现工程深度）

### Q4. 这个评分是一开始就这么设计的吗？

不是，经历了**三代**演进，每一代都被前一代的坑逼出来：

| 代 | 做法 | 被淘汰的原因 |
|---|---|---|
| **第 0 代** jieba 关键词 | 分词→命中计数=分数 | 纯字面匹配，搜不到同义表达，中文分词乱 |
| **第 1 代** LLM 证据评估 | LLM 逐件读证据→打分 | 不稳定/幻觉/贵/慢/黑盒（见 Q5） |
| **第 2 代**（现在 V4）规则 7 维 | relevance 复用 Reranker 分 + 6 维加权 | 确定性、可复现、毫秒级、可解释 |

**代码铁证**：
- jieba→Embedding：[text_retriever.py:3](backend/app/retrieval/text_retriever.py#L3)「V2 重构：替代 jieba」
- LLM→规则：[evidence_metrics.py:1](backend/app/decision/evidence_metrics.py#L1)「替代 LLM relevance」
- V1 时 LLM 是主力：[decision_agent.py:3-7](backend/app/agents/decision_agent.py#L3-L7) 流程第 3 步「LLM 评估」、第 4 步「规则**兜底**」← 兜底二字说明那时规则是配角

### Q5. 为什么不让 LLM 直接打分？它有什么问题？

LLM 打分有 **5 个硬伤**，每个都有代码或系统证据：

| 硬伤 | 证据 | V4 怎么治 |
|---|---|---|
| ① 不稳定/不可复现 | 项目专门建了 eval（Recall/MRR/NDCG）量化波动 | relevance 用 Reranker 分，确定性 |
| ② 幻觉 | [llm_evaluator.py:26](backend/app/retrieval/llm_evaluator.py#L26) prompt 写「不能凭空猜测」← 发生过 | 每分绑 evidence_id，只认真实 chunk |
| ③ 贵且慢 | [llm_evaluator.py:116](backend/app/retrieval/llm_evaluator.py#L116) 日志「completed in X s」 | 评分零 LLM 调用，毫秒级 |
| ④ 黑盒 | — | 输出 `component_scores` 逐维拆解 |
| ⑤ 冷启动/跨品类失真 | — | 贝叶斯平滑 + 28 子品类基准 |

### Q6. relevance 为什么取自 Reranker 而不是 LLM？

- Reranker（Qwen3-Reranker）是**专门训练的 cross-encoder**，判断语义相关度比通用 LLM 更准、更稳、更快、更便宜。
- 而且 Reranker 分在检索阶段**已经算好了**，评分环节直接复用 = **白嫖，零额外 LLM 调用**。
- 代码：[graph.py:314](backend/app/workflow/graph.py#L314) 写入 `reranker_score` → [decision_agent.py:121](backend/app/agents/decision_agent.py#L121) 读取当 relevance。

---

## ⚔️ 模块三：检索-重排-打分-生成 价值论证

### Q7. 传统 RAG 是「检索-重排-生成」，你为什么多一个"打分"？

**本质区别**：传统 RAG 是「找答案」，我们是「做决策」。

| | 传统 RAG | 我们 |
|---|---|---|
| 回答 | 「是什么」（知识问答） | 「买哪个」（推荐决策） |
| LLM 角色 | 裁判 + 播音（全黑盒） | 只播音（规则当裁判） |

**多一个打分 = 把 LLM 从「裁判」降为「播音员」**，让规则当裁判。传统 RAG 让 LLM 一个人干"检索消费+决策+生成"三件事全黑盒；我们拆成「证据当依据、规则当裁判、LLM 当播音」——职责分离。

### Q8. 重排序和你的打分是一回事吗？

**不是。** 这是三个不同的打分阶段：

| 阶段 | 对象 | 方法 | 只回答 |
|---|---|---|---|
| ① 召回 | 全库 | Embedding+Qdrant ANN | 粗筛（不算打分） |
| ② 重排(Reranker) | 候选商品 | cross-encoder 精排 | "**像不像** query"（纯语义） |
| ③ 打分(Decision) | 候选商品 | 7 维加权规则 | "**值不值**得买"（懂业务） |

**关系**：③ 白嫖了②的 `reranker_score` 当自己的 relevance 维度——这就是"relevance 取自 Reranker 精排分"的全部真相。

**秒懂例子**：用户搜"2000 元耳机"，重排把 5000 元旗舰排第一（语义最像），但打分时它超预算 budget_fit 低分，一款 1800 元口碑好的反超排第一。**重排选"最像的"，打分选"最值得买的"。**

### Q9. 针对哪些场景，你这套碾压传统 RAG？

| 场景 | 典型 query | 为什么传统 RAG 不行 |
|---|---|---|
| 多约束决策 | "2000以内、降噪、适合出差" | 有硬约束+多软约束，必须加权权衡 |
| 性价比/比价 | "200块最值得买的面膜" | 要跨商品比"值不值"，重排只懂相关 |
| 横向对比 | "对比这三款" | 需统一评分标尺，LLM 对比易偏颇 |
| 风险敏感 | "敏感肌能用吗""能上飞机吗" | 要风险扣分+绑定差评证据 |
| 冷启动商品 | 新品无评论 | LLM 被少量好评误导，需贝叶斯平滑 |
| 需可解释 | 电商/金融/医疗 | 必须能答"凭什么推荐它" |

**一句话**：凡是「需要权衡多目标、有硬约束、要可解释、风险敏感」的**决策型问答**，我们更优。

### Q10. 什么时候传统 RAG 就够了？

**纯知识问答**——"玻尿酸有什么作用？""退货政策？"这类单事实检索，直接「检索-重排-生成」即可。我们的系统对这类走轻量路径，打分只给**推荐类意图**用。**打分不是取代 RAG，而是给决策型问答加装的裁判大脑。**

---

## 🧮 模块四：打分策略细节

### Q11. 最终那个 0-10 分怎么算出来的？（三层结构）

**第一层：6 维加权主公式**（[scoring.py:167-178](backend/app/decision/scoring.py#L167-L178)）
```
raw = 0.45×relevance + 0.20×budget_fit + 0.12×user_sat
    + 0.10×value_score + 0.08×spec_quality + 0.05×scenario_fit
    + preference_bonus(≤0.10) − risk_penalty(≤0.20) − avoid_penalty(≤0.10)
final = clamp(raw, 0, 1)  →  display = round(final×10, 1)
```

**第二层：证据置信度压顶**（[scoring.py:274-303](backend/app/decision/scoring.py#L274-L303)）——证据不足时压天花板：
- `insufficient_evidence`(证据<0.25) → cap 0.50
- `cautious` → cap 0.80
- `not_recommended` → cap 0.45

**第三层：硬约束一票否决**（[decision_agent.py:168-172](backend/app/agents/decision_agent.py#L168-L172)）——品类/预算×2 不符 → 强制 ≤0.45。

**设计哲学**：加法选人、减法排雷、压顶保底、硬约束砍不合格。

### Q12. 贝叶斯平滑解决什么？

**评论冷启动偏差**。新商品只有 1 条 5 星好评，直接算 = 满分 1.0，比老商品 1000 条 4.8 星(0.96) 还高，荒谬。

公式（[scoring.py:414-416](backend/app/decision/scoring.py#L414-L416)）：`(均分×n + 0.80×3) / (n+3)`
- prior=0.80（先验），C=3（虚拟样本）
- **样本越少越信先验，样本越多越信数据**——经典贝叶斯收缩。

### Q13. 28 个子品类基准价是干嘛的？

**纠正跨品类比价失真**。30 元饮料和 4000 元手机用同一个"全局均价"算性价比，饮料永远满分、手机永远低分。所以每个子品类配 `(中位价, 品质系数)`（[scoring.py:39-68](backend/app/decision/scoring.py#L39-L68)），价格只和**同子品类中位价**比。品质系数体现"每元能买多少品质"（碳酸饮料 1.5，高尔夫 0.9）。

### Q14. evidence_confidence 压顶是什么意思？

防止"分高但无依据"。即使 6 维加权算出 0.92，如果没评论没 FAQ（证据置信度 0.20）→ 判 `insufficient_evidence` → 硬压到 0.50。**用证据质量反向约束打分上限，杜绝虚高推荐。** 公式见 [evidence_metrics.py:314-322](backend/app/decision/evidence_metrics.py#L314-L322)。

### Q15. 每维绑定 evidence_id 有什么用？

让黑盒变白盒。每个分数项带 `method + evidence_ids`（[scoring.py:203-225](backend/app/decision/scoring.py#L203-L225)），风险扣分能指到具体哪条差评（`R-{pid}-{i}`）。能回答"凭什么推荐它"→ 指到具体评论/FAQ。**可解释、可溯源、可审计、无幻觉。**

---

## 🛡️ 模块五：硬核追问防御

### Q16. Reranker 一定会用吗？快速模式怎么办？

不一定。[graph.py:250-255](backend/app/workflow/graph.py#L250-L255)：FAST_MODE 或降级时 Reranker 节点跳过，`reranker_score` 为空，relevance **自动降级为向量检索的余弦相似度**。严谨说法："relevance **优先**取 Reranker 分，不可用时降级为余弦。"

### Q17. 7 维权重 0.45/0.20/... 怎么定的？拍脑袋吗？

体现业务优先级：**先对路再谈值不值**。relevance(0.45) 最大 = 相关度最重要；价格(0.20) 次之；口碑/性价比/规格/场景递减。权重基于电商决策直觉 + eval 评测调优。诚实说：是经验值，但有 eval 指标可迭代。

### Q18. 如果证据全都没有，会怎样？

evidence_confidence → 0 → 判 `insufficient_evidence` → 分数 cap 0.50，且 Response 会生成"证据不足，仅供参考"的提示。系统不会硬推无依据的商品。

### Q19. 怎么证明你的更准？

有 eval 评测系统：10 个 Golden Query + Recall@K / MRR / NDCG@K（[eval.py](backend/app/api/eval.py)）+ Chart.js 仪表盘。对比基线是"LLM 直接打分"，V4 规则评分在**稳定性**（同输入同输出）和**可解释性**上显著更优。诚实说：具体数值需要跑 eval 拿（答辩前建议跑一次存数据）。

### Q20. 这个设计的代价/缺点是什么？（自我反思，必答）

**这是 neuro-symbolic 取舍**：
- 维护成本：7 维权重、贝叶斯参数、28 品类基准需人工标定持续调
- 灵活性：规则硬编码，新场景要改代码，不如 LLM 灵活
- 本质：用「工程复杂度」换「可靠性+可解释性+可复现性」

**收尾金句**：不是打分取代 RAG，而是给"电商推荐决策"这个多约束、需可解释、要可审计的场景，定制了一个 RAG 缺失的确定性裁判内核。

---

## 🚨 防翻车红线（必读）

现场最容易被抓包的 4 个口径，务必记牢：

| 风险点 | 正确口径 | 错误说法 |
|---|---|---|
| **子品类数** | **28 个**（[scoring.py:39](backend/app/decision/scoring.py#L39) 实测） | ~~39 个~~ |
| **"7 维"指什么** | **两层**：6 维加权公式 + 7 个证据质量指标（别混为一谈） | 笼统说"7 维评分" |
| **"不用 LLM"** | **算分不用** LLM；理解(Router/Visual)、生成(Response)照样用 | ~~整个系统不用 LLM~~ |
| **relevance 来源** | **优先** Reranker 分，降级为余弦 | 绝对化说"一定用 Reranker" |

---

## 📎 附录：代码证据速查表

答辩现场被追问"代码在哪"，直接指：

| 论点 | 代码位置 |
|---|---|
| 6 维加权公式 | [scoring.py:167-178](backend/app/decision/scoring.py#L167-L178) |
| 贝叶斯平滑 | [scoring.py:414-416](backend/app/decision/scoring.py#L414-L416) |
| 28 子品类基准 | [scoring.py:39-68](backend/app/decision/scoring.py#L39-L68) |
| 置信度压顶 | [scoring.py:274-303](backend/app/decision/scoring.py#L274-L303) |
| 每维绑 evidence_id | [scoring.py:203-225](backend/app/decision/scoring.py#L203-L225) |
| relevance 取 Reranker 分 | [evidence_metrics.py:225-251](backend/app/decision/evidence_metrics.py#L225-L251) |
| evidence_confidence 公式 | [evidence_metrics.py:314-322](backend/app/decision/evidence_metrics.py#L314-L322) |
| Reranker 节点（重排） | [graph.py:246-350](backend/app/workflow/graph.py#L246-L350) |
| reranker_score 写入 | [graph.py:314](backend/app/workflow/graph.py#L314) |
| 硬约束一票否决 | [decision_agent.py:168-172](backend/app/agents/decision_agent.py#L168-L172) |
| LLM 评估默认关 | [decision_agent.py:63](backend/app/agents/decision_agent.py#L63) `ENABLE_DECISION_LLM` |
| 早期 LLM 打分（活化石） | [llm_evaluator.py](backend/app/retrieval/llm_evaluator.py) |
| jieba→Embedding 演进 | [text_retriever.py:3](backend/app/retrieval/text_retriever.py#L3) |

---

## 🎤 答辩话术节奏建议

1. **开场 30 秒**：背 [§0 电梯陈述](#-0-30-秒电梯陈述开场必背)
2. **被问架构**：走 [Q1→Q2](#-模块一架构与分工)
3. **被问为什么这么设计**：走 [Q4→Q5→Q7](#-模块二三代演进最能体现工程深度)（演进+价值，最有深度）
4. **被问细节**：走 [Q11→Q12→Q13](#-模块四打分策略细节)
5. **被怼缺点**：走 [Q20](#q20-这个设计的代价缺点是什么自我反思必答)（主动反思加分）
6. **全程**：记住 [防翻车红线](#-防翻车红线必读) 4 个口径

> 终极心法：**面试官不爱听"我做了什么"，爱听"我踩了什么坑、为什么这么取舍、怎么演进的"。** 把 LLM 打分的 5 个坑和三代演进讲透，比炫最终架构高级 10 倍。
